================================================================================
数据库课程报告写作指南 & AI 提示词
—— 基于 NexusAI 智能内容采集系统
================================================================================

你的 NexusAI 项目已完美覆盖课程所有核心要求，可直接用作课程报告主题。
以下按老师要求的五个章节组织，每节附 AI 提示词（把 [你的项目路径] 换成实际路径）。

================================================================================
选题建议：基于 MySQL 的智能内容采集与管理系统
================================================================================

你可以选择以下任一角度展开：
1. 基于 MySQL 的内容订阅与采集系统 —— 最简单，聚焦增删改查
2. 基于 MySQL 的多角色审批工作流系统 —— 侧重权限管理
3. 基于 MySQL 的全链路内容管理平台 —— 综合版（推荐）

================================================================================
第一章：系统需求分析 + 概念结构设计(E-R图)
================================================================================

写作要点：
- 描述应用场景：用户订阅内容源→系统自动采集→去重存储→用户查看
- 列出功能需求：注册登录、订阅管理、内容中心、通知中心、审批工单
- E-R 图：6个实体（用户、订阅、内容、审批工单、通知、审计日志）

>>> AI 提示词 1：

```
我有一个 Java + MySQL 的信息管理系统，以下是项目的所有数据库表结构。
请根据这些表，写出课程报告的第一部分：需求分析和概念结构设计。

要求：
1. 写300-500字的需求分析：描述项目背景（智能内容采集管理）、功能需求
   （用户注册登录、订阅规则管理、内容采集与存储、通知推送、审批管理）、
   数据需求
2. 分析系统中的实体和实体间关系，用文字描述 E-R 图结构
3. 用 Mermaid 语法画出 E-R 图（6个实体：user, subscription, content,
   approval_ticket, notification, audit_log），标注每个实体的属性
   和实体间的联系（1:1, 1:N, N:M）

以下是数据库建表SQL（从 D:\NexusAI\infra\sql\init.sql 读取的）：
[把 init.sql 的全部内容粘贴到这里]
```

================================================================================
第二章：逻辑结构设计 + 物理结构设计
================================================================================

写作要点：
- 将 E-R 图转换为关系模式（6张表）
- 分析每张表满足 3NF 的理由（无部分依赖、无传递依赖）
- 列出每张表的字段、类型、约束（主键、外键、唯一、非空、默认值）
- 物理结构：选 InnoDB 引擎、utf8mb4 编码、索引设计

>>> AI 提示词 2：

```
请根据下面的 MySQL 建表 SQL，写出课程报告的第二部分：逻辑结构设计和物理结构设计。

要求：
1. 将 E-R 图转换为关系模式，列出 6 张表的关系模式表示
   （表名(主键用下划线, 外键用波浪线, 其他字段)）
2. 逐一分析每张表满足第三范式(3NF)的理由：
   - 强调每个非主属性完全函数依赖于主键（满足2NF）
   - 强调不存在非主属性对主键的传递函数依赖（满足3NF）
3. 列出每张表的字段设计表（字段名 | 类型 | 长度 | 允许空 | 默认值 | 说明）
   以及约束设计（主键、外键、唯一约束、非空约束、默认值）
4. 物理结构：说明选用 InnoDB 引擎的理由（支持事务、行级锁、外键）、
   utf8mb4 字符集的理由（支持 emoji 和中文）、以及索引设计策略
   （3个复合索引的作用分析）

以下是建表SQL：
[把 init.sql 的全部内容粘贴到这里]
```

================================================================================
第三章：系统功能实现（含图形化界面）
================================================================================

写作要点：
- 技术栈：React + Ant Design（前端）、Spring Boot + MyBatis-Plus（后端）、MySQL
- 描述各模块功能：登录注册、仪表盘、订阅管理、内容中心、审批工单、通知中心
- 附前端页面截图（每个页面一张），说明界面布局和操作流程
- 多角色权限：JWT 认证 + 路由守卫，区分管理员/普通用户

>>> AI 提示词 3：

```
我有一个信息管理系统的前端代码。请根据以下页面组件描述，写出课程报告的
第三部分：图形化交互界面设计与系统功能实现。

要求：
1. 架构描述（100字）：React + Ant Design + Axios 前端，Spring Boot 后端
2. 逐页面描述（每页面150-200字）：
   - 登录/注册页：Tab 切换，表单校验，JWT Token 存储
   - 仪表盘：4个统计卡片（总用户数、订阅数、内容数、待审批数）
   - 订阅管理：表格展示，创建/编辑/删除按钮，Modal 表单
   - 内容中心：搜索+表格，显示ID/标题/平台/作者/是否重复/采集时间
   - 审批工单：待审批列表，通过/拒绝操作
   - 通知中心：通知列表，标记已读/全部已读
3. 每个页面说明界面布局（卡片式布局、左侧导航菜单等）和交互方式

以下是我前端的路由和页面结构：
路由：/login, /dashboard, /subscriptions, /contents, /approvals, /notifications
布局：左侧200px导航栏 + 右侧内容区(白色卡片)
导航项：仪表盘、订阅管理、内容中心、审批工单、通知中心
顶部：用户头像 + 退出登录下拉菜单
```

>>> AI 提示词 4（嵌入式 SQL 实现说明）：

```
请根据我的 Java 项目，写出嵌入式 SQL 实现方式的说明。我的项目使用
MyBatis-Plus（Spring Boot 的 ORM 框架），底层通过 LambdaQueryWrapper
和 BaseMapper 执行 SQL 操作。

请写 400-500 字说明：
1. MyBatis-Plus 如何将 Java 方法调用映射为 SQL 语句
2. 以 SubscriptionServiceImpl 为例，说明 create() 方法中的 insert 操作
  和 list() 方法中的 select + 分页 + 条件查询操作
3. 如何通过 LambdaQueryWrapper 实现安全的条件拼接（防 SQL 注入）
4. 事务管理：Spring @Transactional 注解确保数据一致性

以下是核心代码片段：
[把 SubscriptionServiceImpl.java 的 create() 和 list() 方法粘贴到这里]
```

================================================================================
第四章：数据库交互与数据安全
================================================================================

写作要点：
- 增删改查实现方式（MyBatis-Plus / 嵌入式 SQL）
- 数据校验（前端表单校验 + 后端 @Valid 注解）
- 事务处理（Spring 声明式事务）
- 安全设计：密码 BCrypt 加密、JWT 认证、水平越权防护

>>> AI 提示词 5：

```
请根据以下信息，写出课程报告的第四部分：数据库交互与数据安全。

要求（共600-800字，分3个小节）：

1. 嵌入式SQL数据操作（300字）：
   - 以订阅管理为例，说明增删改查的 SQL 映射方式
   - MyBatis-Plus 的 ServiceImpl 基类提供 save/updateById/removeById/getById
     等方法，底层自动生成 INSERT/UPDATE/DELETE/SELECT 语句
   - 分页查询使用 Page 对象 + LambdaQueryWrapper 条件拼接

2. 数据安全与权限控制（300字）：
   - 密码 BCrypt 加密存储，不可逆
   - JWT Token 认证：登录后签发，请求头携带，Gateway 全局校验
   - 水平越权防护：从 UserContext（ThreadLocal）获取当前用户ID，
     而非从请求参数获取（对比修复前后的代码）
   - 垂直权限：区分 admin/user 角色

3. 事务一致性（100字）：
   - Spring @Transactional 声明式事务
   - 示例：trigger() 方法中获取分布式锁 + 更新 last_run_at + 发送消息
     在同一事务中完成

以下是安全相关代码：
- JwtAuthenticationInterceptor.java：[把拦截器代码粘贴到这里]
- 修复前的漏洞代码（@RequestParam Long userId）与修复后（UserContext.getUserId()）
```

================================================================================
第五章：系统测试与实验总结
================================================================================

>>> AI 提示词 6：

```
请根据下面描述的测试用例，写出课程报告的第五部分：系统测试与实验总结。

要求：
1. 设计 5 个功能测试用例，每个测试用例包含：
   - 测试编号、测试模块、测试项、前置条件、操作步骤、预期结果、实际结果
   - 用表格形式呈现
2. 测试用例覆盖：
   TC01：用户注册（输入用户名密码→注册成功→数据库新增记录）
   TC02：用户登录（正确密码→获取Token→跳转仪表盘；错误密码→提示错误）
   TC03：创建订阅规则（登录→进入订阅管理→点击创建→填写表单→提交→列表刷新）
   TC04：触发内容采集（选择订阅→点击触发采集→查看内容中心新增文章）
   TC05：数据校验（订阅名称为空→提交失败→提示"规则名称不能为空"）
3. 实验总结（200字）：总结项目中学到的数据库设计、SQL优化、系统开发经验，
   以及可以改进的方向

测试截图建议：
- 前端各页面截图（登录、注册、仪表盘、订阅管理、内容中心）
- 数据库 Navicat/HeidiSQL 截图（表结构、查询结果）
- 关键功能操作前后的对比截图
```

================================================================================
附录：项目技术栈速查表
================================================================================

| 类别       | 技术                                      |
|------------|-------------------------------------------|
| 数据库     | MySQL 8.0, InnoDB, utf8mb4               |
| 后端框架   | Spring Boot 3.2.5, MyBatis-Plus 3.5.7    |
| 安全认证   | JWT (jjwt 0.12.5), BCrypt                 |
| 前端框架   | React 18, Ant Design 5, Vite, TypeScript  |
| 状态管理   | Zustand (persist 持久化)                  |
| HTTP 通信  | Axios (拦截器自动附加 Token)              |
| 消息队列   | RocketMQ (可选)                           |
| 向量搜索   | Milvus (可选)                             |
| 缓存       | Redis                                    |
| 数据库表数 | 6 张                                     |
| 总实体数   | 40+ (Java entity fields)                 |

================================================================================
注意事项
================================================================================

1. 文件名：学号-姓名.docx（不要忘了）
2. 系统源码放在压缩包里作为附件提交
3. SQL 脚本从 infra/sql/init.sql 直接复制
4. 前端截图用浏览器的 F12 → 截屏或 Win+Shift+S
5. 数据库截图用 HeidiSQL/MySQL Workbench 截取
6. 报告总字数控制在 3000-5000 字，不要写太多
7. E-R 图用 Mermaid 画最简单，或者用 draw.io 截图

================================================================================
